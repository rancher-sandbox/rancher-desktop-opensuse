[Unit]
Requires=network-namespace.service network-setup.service
After=network-setup.service

[Service]
ExecStart=
ExecStart=/usr/local/bin/wsl-exec \
  /usr/bin/dockerd \
  --add-runtime oci=/usr/sbin/runc \
  $DOCKER_OPTS
